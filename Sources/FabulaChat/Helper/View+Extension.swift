import SwiftUI

extension View {
    /// Rotates the current view by `pi`
    public func flip() -> some View {
        return self
            .rotationEffect(.radians(.pi))
            .scaleEffect(x: -1, y: 1, anchor: .center)
    }
}
